## API Report File for "professional-ts"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import * as React_2 from 'react';

// @public
export function apiCall(path: string, init?: RequestInit): Promise<unknown>;

// @public
export class Deferred<T> {
    constructor();
    // (undocumented)
    get promise(): Promise<T>;
    // (undocumented)
    get reject(): RejectHandler;
    // (undocumented)
    get resolve(): ResolveHandler<T>;
}

// @public
export function formatTimestamp(date: Date): string;

// @public
export class HTTPError extends Error {
    constructor(info: Response, message: string);
    // Warning: (ae-forgotten-export) The symbol "HTTPErrorKind" needs to be exported by the entry point public-api-surface.d.ts
    //
    // (undocumented)
    kind: HTTPErrorKind;
}

// @public
export interface IChannel {
    // (undocumented)
    description: string;
    // (undocumented)
    id: string;
    // (undocumented)
    messages: IMessage[];
    // (undocumented)
    name: string;
    // (undocumented)
    teamId: string;
}

// @public
export interface IMessage {
    // (undocumented)
    body: string;
    // (undocumented)
    channelId: string;
    // (undocumented)
    createdAt: string;
    // (undocumented)
    id: number;
    // (undocumented)
    teamId: string;
    // (undocumented)
    user: IUser;
    // (undocumented)
    userId: string;
}

// @beta
export function isChannel(arg: any): arg is IChannel;

// @beta
export function isMessage(arg: any): arg is IMessage;

// @beta
export function isTeam(arg: any): arg is ITeam;

// @public
export function isTypedArray<T>(arr: unknown, check: (x: any) => x is T): arr is T[];

// @public
export interface ITeam {
    // (undocumented)
    channels: IChannel[];
    // (undocumented)
    iconUrl: string;
    // (undocumented)
    id: string;
    // (undocumented)
    name: string;
}

// @public
export interface IUser {
    // (undocumented)
    iconUrl: string;
    // (undocumented)
    id: number;
    // (undocumented)
    name: string;
    // (undocumented)
    username: string;
}

// @public (undocumented)
export type RejectHandler = (reason: unknown) => void;

// @public (undocumented)
export type ResolveHandler<T> = (value: T | PromiseLike<T>) => void;

// @public
export const TeamSidebar: React_2.FunctionComponent<{
    team: ITeam;
}>;


// (No @packageDocumentation comment for this package)

```
